server:
  port: 7001

eureka:
  instance:
    hostname: localhost #Eureka服务端的实例名称
  client:
    register-with-eureka: false #是否注册自身
    fetch-registry: false #false表示该服务为注册中心
    service-url: #监控页面的地址
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/